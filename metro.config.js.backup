// Workspace root Metro config - redirects to mobile package
const path = require('path');

// Get the mobile package's metro config
const mobileMetroConfig = require('./packages/mobile/metro.config');

module.exports = {
  ...mobileMetroConfig,
  // Ensure the project root is the mobile package
  projectRoot: path.resolve(__dirname, 'packages/mobile'),
};
